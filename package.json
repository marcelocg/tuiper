{
  "name": "tuiper",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:corpus": "bun run scripts/build-corpus.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "yaml": "^2.9.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.4.2"
  }
}
